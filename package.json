{
  "name": "bottlefolio-gate",
  "private": true,
  "description": "The tools the gate runs, pinned so every cloud run is the same run. Nothing here is loaded by index.html.",
  "devDependencies": {
    "eslint": "8.57.1",
    "google-auth-library": "11.0.2",
    "playwright": "1.63.0"
  }
}
